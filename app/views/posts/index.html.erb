<div class="main posts-index">
  <div class="container">
    <% @posts.each do |post| %>
        <div class="posts-index-item">
          <%= post.id %>
          <%= post.content %>
          <%= post.created_at %>
        </div>
    <% end %>
  </div>
</div>

